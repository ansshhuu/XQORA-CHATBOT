GROQ_API_KEY=
RESEND_API_KEY=
TEAM_EMAIL=

# Comma-separated list of domains allowed to call this API (CORS). Replace
# with XQORA's real production domain(s) before deploying — never use "*".
CORS_ORIGINS=https://xqora.com,https://www.xqora.com

# Optional overrides (defaults shown in app/core/config.py)
# GROQ_MODEL=llama-3.3-70b-versatile
# DATABASE_URL=sqlite:///./xqora_chatbot.db
# RATE_LIMIT_PER_MINUTE=10
# MAX_MESSAGE_LENGTH=1000
